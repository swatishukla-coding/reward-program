server:
  port: 8080

spring:
  application:
    name: rewards-program
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false

logging:
  level:
    root: INFO
    com.retailer.rewards: DEBUG
